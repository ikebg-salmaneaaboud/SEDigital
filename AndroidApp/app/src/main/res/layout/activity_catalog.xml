<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout: activity_catalog.xml
    Descripción: Pantalla principal del catálogo de juegos.
    Estructura:
      - ConstraintLayout raíz con fondo de degradado vertical.
      - Cabecera reutilizable (header_with_cart) con carrito y título.
      - RecyclerView para mostrar la lista de juegos, adaptable mediante un Adapter.
      - Barra de navegación inferior incluida desde view_bottom_nav.xml para consistencia en la app.
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:fitsSystemWindows="true"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/vertical_gradient"
    tools:context=".ui.catalog.CatalogActivity">

    <include
        android:id="@+id/main_header"
        layout="@layout/header_with_cart"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerGames"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/main_header"
        app:layout_constraintBottom_toTopOf="@+id/bottomNavigationView" />

    <include
        layout="@layout/view_bottom_nav"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/bottomNavigationView"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
