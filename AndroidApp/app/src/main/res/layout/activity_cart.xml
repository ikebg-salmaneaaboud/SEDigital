<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout: activity_cart.xml
    Descripción: Pantalla del carrito de compras.
    Estructura:
      - ConstraintLayout raíz con fondo de degradado vertical.
      - Cabecera reutilizable (header_with_return) con icono para volver a la pantalla anterior.
      - RecyclerView para mostrar los elementos del carrito, adaptable mediante CartAdapter.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:fitsSystemWindows="true"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/vertical_gradient"
    tools:context=".ui.cart.CartActivity">

    <!-- Cabecera con botón de regreso -->
    <include
        android:id="@+id/main_header"
        layout="@layout/header_with_return"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- RecyclerView que muestra los items del carrito -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerCart"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/main_header"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
